<%= simple_form_for(@user, html: { class: 'form-vertical' }) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.input :username, placeholder: 'Enter username' %>
  <%= f.input :email, placeholder: 'Enter email' %>
  <%= f.input :password, :label => 'Password',:placeholder => 'Password' %>
  <%= f.input :password_confirmation, :label => 'Confirm', placeholder: 'Confirm Password' %>
  <%= f.button :submit %>
  <% end %>
  